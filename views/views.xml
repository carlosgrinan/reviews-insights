<odoo>
    <data>

        <!-- actions -->
        <record model="ir.actions.client" id="dashboard">
            <field name="name">Dashboard</field>
            <field name="tag">reviews_insights.dashboard</field>
        </record>

        <!-- menu items -->
        <menuitem name="Reviews Insights" id="reviews_insights.menu_root" groups="base.group_user" action="reviews_insights.dashboard" />

    </data>
</odoo>